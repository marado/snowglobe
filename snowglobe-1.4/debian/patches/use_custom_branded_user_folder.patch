diff --git a/1.3/indra/newview/llappviewer.cpp b/1.3/indra/newview/llappviewer.cpp
index 2a8a2f8..ba9e6e9 100644
--- a/1.3/indra/newview/llappviewer.cpp
+++ b/1.3/indra/newview/llappviewer.cpp
@@ -558,7 +558,7 @@ bool LLAppViewer::init()
 	
 	// Need to do this initialization before we do anything else, since anything
 	// that touches files should really go through the lldir API
-	gDirUtilp->initAppDirs("SecondLife");
+	gDirUtilp->initAppDirs("snowglobeOMV");
 	// set skin search path to default, will be overridden later
 	// this allows simple skinned file lookups to work
 	gDirUtilp->setSkinFolder("default");
